#!/usr/bin/env bash
set -e -x -o pipefail

rm -rf "${LOCAL_BUILD_PATH}"

mkdir -p "${LOCAL_BUILD_PATH}"

yes | cp -rf scripts/{install,add-site,enable-ssl,delete-ssl}.sh "${LOCAL_BUILD_PATH}"

tar -zcxvf "${LOCAL_BUILD_PATH}/playbook.tar.gz" playbook.yml ansible.cfg roles vars
