#!/usr/bin/env bash
set -e -x -o pipefail

rm -rf "${LOCAL_BUILD_PATH}"

mkdir -p "${LOCAL_BUILD_PATH}"

cp scripts/*.shhttps://gitlab.x.apli.tech/apliteni/keitaro/installer/-/merge_requests/251/diffs "${LOCAL_BUILD_PATH}"

tar -zcvf "${LOCAL_BUILD_PATH}/playbook.tar.gz" playbook.yml ansible.cfg roles vars
