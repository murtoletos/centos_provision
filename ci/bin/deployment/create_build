#!/usr/bin/env bash
set -e -x -o pipefail

rm -rf "${LOCAL_BUILD_PATH}"

mkdir -p "${LOCAL_BUILD_PATH}/${ANSIBLE_FOLDER}"

cp scripts/*.sh "${LOCAL_BUILD_PATH}"

zip -r "${LOCAL_BUILD_PATH}/playbook.zip" playbook.yml ansible.cfg roles vars
